# Vue Router history 模式 — aaPanel 伪静态规则
# 面板路径：网站 → mechbox.cax.do → 设置 → 伪静态 → 粘贴以下内容

location / {
    try_files $uri $uri/ /index.html;
}

location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    expires 7d;
    add_header Cache-Control "public, immutable";
}
